-- Бейджи «Новое» и «Бесплатно» убраны из интерфейса, поэтому колонка,
-- которую больше никто не читает и не пишет, уходит из схемы.
--
-- Миграция необратима: значения бейджей не сохраняются. Восстанавливать
-- нечего — метки выставлялись вручную и смысла вне карточки не имели.

begin;

alter table public.posts drop column if exists badges;

commit;
